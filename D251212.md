# 과제에 필요한 내용 복습

## 필수 문제 1에  필요한 개념
- 클래스 만들기
    - 클래스 구성요소
        - 생성자(객체 속송을 초기화하는 특별한 함수)
        - 매서드(함수)
        - 속성(클래스 내에 있는 변수, aka 멤버변수)
```dart
// class 클래스명 { 이 안에 구성요소 배치 }
class Score {
    // 1. 속성 : 점수(숫자)
    int point;
    // 2. 메서드 : 점수(속성 중 하나)를 출력하는 기능
    // 반환타입 함수이름 (매개변수) {실행할 코드}
    void showInfo() {
        print("점수: $point");
    }
    // 3. 생성자 : 점수를 초기화
    Score(this.point);
}    
```
- 상속
    - 특정 클래스의 속성과 메서드들을 모두 포함하여 확장해서 구현하고 싶을 때!
    - `extends` 키워드 사용
```dart
class StudentScore extends Score {
    // Score 클래스의 구성요소가 포함됨
    // 📌 상속은 반드시 하나의 클래스만 가능!
    // 현재 속성 : point, 메서드 : showInfo 가지고 있음!
    String name;
    
    // @override => 어노테이션 => 코드의 메타데이터 => 안붙여도 문제 없음 => 재정의 했다는 것을 다른 개발자에게, 혹은 자신에세 보기 편하게 하려고 강력히 사용 권장!
    @override
    void showInfo(){
        print("이름 : $name, 점수 : $point");
    }
    
    
    // 생성자
    StudentScore(this.name, super.point);
}
```    
## 필수 문제 2에 필요한 개념
- 파일 입력
```dart
import 'dart.io'

void main(){
    // 1. 파일객체 생성
    File txt = File("students.txt");
    // 2. 문자열로 불러오기
    String txtString = await txt.readAsString();  
}
```
- 문자열 가공
    - `split` 함수가 가장 많이 사용됨
        - `String` 클래스가 가지고 있는 함수
        - "문자열".split(구분 할 문자)
        - "문자열, 문자열2".split(",") => ["문자열", "문자열2"]
## 필수 문제 3에 필요한 개념
- 사용자 입력
    - `stdin.realLineSync{}` 함수를 문자열로 받을 수 있음
- 조건문 / 반복문
    - `for`, `
## 필수 문제 4에 필요한 개념
- 파일 출력
